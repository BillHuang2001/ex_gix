[package]
name = "ex_gix"
version = "0.1.0"
authors = []
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rustler = "0.37.2"
gix = { version = "0.80", default-features = false, features = [
    "revision",
    "parallel",
    "attributes"
] }
